## yield 让权
以下是为你提炼的核心本质，可直接摘录到你的笔记（如 Obsidian）中：

- **第一句（底层机制）：** `yield` 的本质是主动触发一次不改变自身就绪状态的上下文切换，并将当前任务强行挪到**同等优先级就绪链表的尾部**。
    
- **第二句（运行逻辑）：** 它是同优先级任务间共享 CPU 的让权机制；若全场无其他同级或更高级任务，调度器在轮询后仍会**原地切回**当前任务继续运行。

