---
tags:
  - guide
status: evergreen
---

# 🧠 记忆复习指南（Spaced Repetition）

> 为什么：艾宾浩斯遗忘曲线告诉我们，学过的东西 24h 内忘掉 ~70%。**间隔重复（SRS）+ 主动回忆（Active Recall）** 是已知对抗遗忘最有效的方法，比"重读笔记"效率高一个量级。
> 工具：Obsidian 插件 **Spaced Repetition**（已安装，v1.15.4）。

## 一、怎么把知识点变成卡片

打开任意笔记，在知识点**所在行**添加 SRS 语法：

### 1. 单行卡片（最常用）
```markdown
- `cdev_add()` 的作用？:: 将设备号和 cdev 对象绑定并加入 cdev_map #flashcards
```

### 2. 多行卡片（问题/答案分行）
```markdown
为什么 NAPI 能缓解中断风暴？
?
中断合并 + 轮询，减少中断次数，用"事件驱动"换"CPU 占用" #flashcards
```

### 3. 填空/挖空（Cloze）
```markdown
`device_create` 的三个阶段：摇号（{{c1::alloc_chrdev_region}}）、赋值（{{c2::cdev_add}}）、挂牌（{{c3::device_add}}） #flashcards
```

> **规则**：一张卡片只考**一个**知识点；优先做"易混淆对比"和"API 定义"类卡片（你的笔记里这类内容极多）。

## 二、怎么复习

1. `Ctrl+P` → **Spaced Repetition: Review flashcards**（或左侧面板按钮）
2. 逐张自答 → 点 **Again / Hard / Good / Easy**（决定下次间隔）
3. 默认间隔：Again≈1 分钟，Hard≈10 分钟，Good≈1 天，Easy≈4 天；之后按 2× 递增

## 三、建议节奏（学习闭环）

| 时机 | 动作 |
|---|---|
| 学习当天 | 给 5-10 个核心知识点做卡片，学完立刻复习一轮（Good 起步） |
| 第 1-3 天 | 每天 Review 一次（Good/Easy） |
| 第 7 天 | 只复习 Again/Hard 的卡片 |
| 第 21 天 | 抽查"易混淆点"，能秒答就 Easy |

## 四、配合本库已有资产

- **费曼笔记**：写笔记时"用类比讲清楚"的部分 → 做成 `问题？:: 类比一句话` 卡片，记忆锚点最强
- **Mermaid 图**：图的**关键结论**做成卡片（图本身不用背，结论要背）
- **Dashboard 复习队列**：`00-DASHBOARD` 自动列出所有含 `#flashcards` 的笔记，随时看哪些还没做卡片

## 五、常见问题

- **卡片不显示**：检查是否在行内有 `#flashcards` 标签（或在插件设置里改默认标签）
- **复习太快**：把插件的"间隔倍率"调低，别贪多，每天 ≤20 张
- **哪篇先做**：`00-TODO` 列出的 todo 笔记（学习未完）优先；`status: reference` 的参考笔记不用做
