## 1. 对prompt的最新理解
 - Prompt 不是“让模型做事的一句话”，而是“对模型行为进行约束编排的轻量程序”
 - Prompt 趋向结构化、规范化、可复用的文本，不应是一句随意的聊天文本
 - Prompt的抽象公式：```Prompt = Role + Objective + Context + Constraints + Process + OutputFormat + InputBinding + QualityBar```
 - 