
# 驱动开发-设备定义-注册-泛化调用 流程个人理解笔记
## phase 1: 根据设备框架模型开发driver，实现所选的设备对象

## phase 2：申请全局唯一的设备ID，这是个单独的管理模块，实现发放ID和唯一性保证

## phase 3：基于一切皆文件的设计思路，添加到VFS(被分配的设备ID + 开发的字符设备driver对象<本质是operations>)，此时根据driver中定义的device name会在 file system中加入inode；
## phase 4：添加设备对象和设备ID到 cdev_map，支持通过ID获取cdev指针

## phase 5： 泛化的具体实现。执行open(path of device), 文件系统打开指定文件，动态创建struct file，获取inode，取到 设备ID，然后检索 cdev_map，获取cdev指针

## phase 6： 